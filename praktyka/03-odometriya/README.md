# Робота 3. Чому робот не їде рівно

Тиждень 3 · до **5 балів** · на парі — до 14:15 · не були на парі — до початку наступного заняття

---

## Мета

Виміряти, **на скільки насправді** повертається робот, коли ви кажете йому
«повернись на 90°». Двома способами — «на час» і «по гіроскопу» — по десять
разів. Отримати **свої** числа й пояснити їх.

⭐ Ви отримаєте десять різних чисел там, де чекали десять однакових.
Це не помилка у вас. Це точка, з якої починається весь курс, і ці числа
знадобляться ще на тижні 15.

---

## Мінімум для зарахування

- [ ] `zvit.md` у цій теці — **дві таблиці по 10 поворотів**: «на час» і «по гіроскопу»
- [ ] під таблицями — **середнє** й **розкид** (max − min) для кожного способу
- [ ] **три речення гіпотези** (крок 5)
- [ ] рядок у `journal.md`, тиждень 3 — з числами
- [ ] коміт і `git push`

## Контрольні точки на парі

| Час | Має бути готово | Що написати в чат |
|---|---|---|
| 12:45 | кроки 0–3 | `на час: середнє / розкид` |
| 13:30 | крок 4 | `на час: … / … · гіроскоп: … / …` |
| 14:00 | — | номер кроку, на якому ви зараз: `5`, `⭐2`… |
| 14:15 | крок 5, push | посилання на свій `zvit.md` |

**Перші четверо**, хто напише в чат рядок після кроку 4, показують роботу на розборі.

---

## Карта кроків

| Крок | Що | Хв |
|---|---|---|
| 0 | Gears, каркас програми | 5 |
| 1 | прогноз одним числом | 5 |
| 2 | 10 поворотів «на час» | 20 |
| 3 | середнє, розкид, систематична похибка | 15 |
| 4 | 10 поворотів «по гіроскопу» | 25 |
| 5 | гіпотеза, журнал, коміт | 20 |
| ⭐ | драбина й треки — хто закінчив раніше | решта |

---

## Крок 0. Каркас — 5 хв

1. Відкрити [Gears](https://gears.aposteriori.com.sg). Світ і робот — ті, що
   відкриваються самі: світ **Grid**, робот **Single Sensor Line Follower**.
   Якщо міняли — **Worlds → Select World → Grid** і **Robot → Select Robot**.
2. Вкладка **Python**, стерти все й вставити:

```python
from ev3dev2.motor import MoveTank, OUTPUT_A, OUTPUT_B, SpeedPercent
from ev3dev2.sensor import INPUT_3
from ev3dev2.sensor.lego import GyroSensor
import time

tank = MoveTank(OUTPUT_A, OUTPUT_B)   # A — ліве колесо, B — праве
gyro = GyroSensor(INPUT_3)            # ⚠️ у Gears порт обов'язковий
time.sleep(0.5)

print("гіроскоп:", gyro.angle)
```

3. Вкладка **Simulator** → ▶. Консоль — унизу екрана симулятора
   (якщо згорнута — стрілка `^` праворуч унизу).

✔ У консолі: `гіроскоп: 0`

---

## Крок 1. Прогноз — 5 хв

Нічого не запускаючи, запишіть у `zvit.md` одне число:
робот крутиться на місці **1 секунду** на швидкості **20 %**.
**На скільки градусів він повернеться?**

✔ Число записане **до** запуску. Правильність прогнозу не оцінюється —
оцінюється, що він є і що потім ви порівняли його з виміром.

---

## Крок 2. Поворот «на час», 10 разів — 20 хв

**Мета:** робот десять разів поспіль повертається праворуч «на час», а
програма після кожного повороту друкує, на скільки він **справді** повернувся.

<details>
<summary>Підказка — код циклу, дописати під каркас</summary>

```python
poperednii = gyro.angle
for i in range(1, 11):
    tank.on_for_seconds(SpeedPercent(20), SpeedPercent(-20), 1.0)
    time.sleep(0.5)                  # дати роботу заспокоїтись
    teper = gyro.angle
    print(i, teper - poperednii)     # кут САМЕ ЦЬОГО повороту
    poperednii = teper
```

</details>

Перенести десять чисел у стовпчик «на час» таблиці в `zvit.md` (заготовка нижче).

✔ Десять чисел, і вони **не 90 і не однакові**.

⚠️ Записуйте різницю з попереднім показом, а не сам `gyro.angle`: після
четвертого повороту він уже під 300.

---

## Крок 3. Середнє, розкид, систематична похибка — 15 хв

| Що | Як порахувати | Що це означає |
|---|---|---|
| **середнє** | сума десяти / 10 | куди робот «тягне» щоразу |
| **систематична похибка** | середнє − 90 | повторюється → її **можна компенсувати** |
| **розкид** | найбільше − найменше | випадкова частина → **не компенсується** |

✔ Під стовпчиком «на час» — три числа. Порівняйте середнє зі своїм прогнозом.

---

## Крок 4. Поворот «по гіроскопу», 10 разів — 25 хв

**Мета:** робот повертається, **поки гіроскоп не покаже 90°**. Десять разів,
перед кожним поворотом гіроскоп обнуляється. Порахувати те саме: середнє й розкид.

<details>
<summary>Підказка — код циклу, замінити цикл кроку 2</summary>

```python
for i in range(1, 11):
    gyro.reset()                     # обнулити перед кожним поворотом
    tank.on(SpeedPercent(20), SpeedPercent(-20))
    while gyro.angle < 90:           # праворуч — кут зростає
        pass
    tank.off()
    time.sleep(0.5)
    print(i, gyro.angle)
```

</details>

✔ Числа ближчі до 90, але **більші** за 90, а розкид менший, ніж у кроці 2.

---

## Крок 5. Гіпотеза, журнал, коміт — 20 хв

У `zvit.md` — три речення, по одному на питання:

1. Чому за **однакової** програми числа різні?
2. Що у ваших числах **систематичне**, а що **випадкове**?
3. Чому по гіроскопу робот **перекручує**, хоча умова — «менше 90»?

Рядок у `journal.md`, тиждень 3 — з числами. Потім:

```bash
git add .
git commit -m "Робота 3: таблиці похибок повороту"
git push
```

✔ На GitHub у `praktyka/03-odometriya/` видно `zvit.md` з двома таблицями.

---

## Заготовка `zvit.md`
# Робота 3 — похибка повороту

Прогноз (крок 1): 110°

| № | на час, ° | по гіроскопу, ° |
|---|---|---|
| 1 | 118 | 94 |
| 2 | 116 | 93 |
| 3 | 119 | 95 |
| 4 | 115 | 93 |
| 5 | 117 | 94 |
| 6 | 118 | 93 |
| 7 | 116 | 95 |
| 8 | 120 | 94 |
| 9 | 117 | 93 |
| 10 | 116 | 94 |
| **середнє** | **117.2** | **93.8** |
| **розкид** | **5** | **2** |

Систематична похибка «на час»: +27.2°

## Гіпотеза

1. Числа виходять різними за однакової програми через дискретність обчислювального циклу симуляції (затримки та часові кроки фізичного рушія), варіації тертя коліс об поверхню та квантування вимірювань датчика.
2. У цих числах **систематичною похибкою** є постійне відхилення від цільового кута (середній перекрут +27.2° для повороту за часом та +3.8° для гіроскопа), обумовлене геометричними параметрами робота й інерцією; **випадковою похибкою** є розкид (5° та 2°), викликаний нерівномірністю часу зупинки двигунів між циклами.
3. При використанні гіроскопа робот перекручує кут (в середньому ~94° замість 90°), оскільки після виходу з циклу `while gyro.angle < 90` та виклику `tank.off()` фізичний робот продовжує рухатися за інерцією ще кілька мілісекунд до повної зупинки.

## Розбір (якщо робота здається поза парою)

1. Найбільше здивувало те, що при керуванні «на час» робот замість очікуваних 90° повертається майже на 117° — реальна швидкість обертання робота виявилася значно вищою за розраховану суб'єктивно.
2. Розкид по гіроскопу менший за часовий, але не нульовий, оскільки момент опитування датчика в циклі програмування та швидкість спрацьовування зупинки двигунів мають незначні часові затримки.
3. Якби робот проїхав 10 таких поворотів поспіль «на час», то замість підсумкових 180° (після 10 поворотів по 90° у квадрат/коло) він сумарно повернувся б на $117.2^\circ \times 10 = 1172^\circ$. В результаті замість початкового напрямку він відхилився б на $1172^\circ \bmod 360^\circ = 92^\circ$, тобто опинився б практично перпендикулярно до очікуваного курсу.
---

## Якщо щось пішло не так

| Що бачите | Причина | Що зробити |
|---|---|---|
| `TypeError: No gyro sensor connected to null` | датчик створено без порту | `GyroSensor(INPUT_3)` і `from ev3dev2.sensor import INPUT_3` |
| `No gyro sensor connected to in3` | вибрано іншого робота | **Robot → Select Robot** → Single Sensor Line Follower |
| консоль порожня | запущено не з вкладки Simulator або консоль згорнута | вкладка **Simulator**, ▶, стрілка `^` унизу |
| кут завжди `0`, робот стоїть | програма впала до руху — дивіться червоний рядок у консолі | виправити помилку, ▶ ще раз |
| у кроці 4 робот крутиться без кінця | крутиться **ліворуч**, кут від'ємний | швидкості `20, -20` (саме в такому порядку) |
| числа 90, 180, 270… у кроці 2 | записуєте `gyro.angle`, а не різницю | `teper - poperednii` |
| `IndentationError` | змішані пробіли й табуляція | відступи — 4 пробіли |
| у вас не такі числа, як у сусіда | так і має бути | це тема розбору в третій парі |

---

## ⭐ Драбина — хто закінчив раніше

Беріть по черзі, не чекайте. Кожен щабель — рядок у `zvit.md`: що зробили,
число, висновок.

| # | Завдання | Хв |
|---|---|---|
| ⭐1 | **Швидкість.** Поворот по гіроскопу на 10 % і на 40 %, по 5 разів. Спершу прогноз: де перекрут більший і чому? | 10 |
| ⭐2 | **Точний поворот.** Дві швидкості: 30 % до 70°, потім 5 % до 90°. Мета — рівно 90° у п'яти поворотах із п'яти | 15 |
| ⭐3 | **Кут з коліс.** `tank.left_motor.position` — градуси обертання колеса. Шлях колеса $s = \frac{\text{градуси}}{360}\cdot\pi\cdot 5{,}6$ см, кут $\Delta\theta = \frac{s_L - s_R}{b}$, $b = 15{,}2$ см. Порівняйте з гіроскопом і знайдіть, за якої $b$ числа збігаються. Чому вона не 15,2? | 20 |
| ⭐⭐4 | **Справжня задача — повернутися на базу.** Робот проїжджає 40 см → повертає праворуч на 90° → 30 см → праворуч ще на 135° → 25 см, увесь час рахуючи свої $(x, y)$ лише з коліс і гіроскопа. Потім **сам** розвертається до старту й доїжджає. Промах вимірюєте GPS (`GPSSensor(INPUT_4)`, `gps.x`, `gps.y`, у см) — але в розрахунку руху GPS **не використовувати**. Мета — промах ≤ 3 см | 40+ |

⭐⭐4 — це місія № 11 з матриці варіантів у мініатюрі й шматок теми 15.

## Трек П

**Компенсація.** Нова тривалість повороту = `1.0 · 90 / ваше_середнє`.
Ще 10 поворотів «на час» із новою тривалістю. Що сталося із середнім?
А з розкидом? Поясніть, чому одне число вдалося виправити, а друге — ні.

## Трек Д

**Слід робота.** У стандартного робота на порту 5 є перо:

```python
from ev3dev2.sensor import INPUT_5
from ev3dev2.sensor.virtual import Pen

pen = Pen(INPUT_5)
pen.down()        # робот малює свій шлях на підлозі
```

Проїхати квадрат **тричі поспіль** (2 с уперед, поворот «на час»), зробити
скріншот сліду, покласти поруч із `zvit.md`. Трьома реченнями: що бачить
глядач, і чим слід відрізняється від того, що робот «думає» про свій шлях.

---

## Не були на парі або не встигли

- ті самі кроки 0–5, дедлайн — **початок наступного заняття**;
- замість показу на розборі — у `zvit.md` окремий розділ **«Розбір»**,
  відповіді на три питання:
  1. Яке число у ваших таблицях найбільше здивувало і чому?
  2. Чому розкид по гіроскопу менший, але не нульовий?
  3. Якби робот мав проїхати 10 таких поворотів поспіль «на час», де б він
     опинився відносно очікуваного курсу — порахуйте з вашого середнього;
- посилання на `zvit.md` — у чат дисципліни.

---

## RoboMind

**Basics 1 — дедлайн перенесено на тиждень 6 (07.10).** Прогрес видно викладачу на платформі,
окремо здавати нічого не треба.

---

## Критерії — 5 балів

| Балів | За що |
|:---:|---|
| 2 | Дві таблиці по 10 поворотів, середнє й розкид для кожної |
| 2 | ⭐ Пояснюєте, що в числах систематичне, що випадкове і чому по гіроскопу перекручує |
| 1 | Рядок у `journal.md` з числами |

⚠️ Три повороти замість десяти — це не таблиця, а три числа. Розкид на трьох не видно.
